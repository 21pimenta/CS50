#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

int main(void)
{
    int days;
    int first;
    long long total;
    long double totaldollars;

    // vars

    // get a number of days
    do
        days = get_int("Days in month: ");
    while (days < 28 || days > 31);
        // check days is between 28 and 31
    // get a number of pennies
    pennies = 1;
    // add double the amt of pennies of the previous day eachday
    for (int = 1; i < days; i++);
    {

    }
    // output the total
}